// Entity public API.
// Re-export types, domain functions, shared queries, and query keys.
//
// export type { {{pascalCase (singular name)}}, {{pascalCase (singular name)}}ListItem } from "./{{kebabCase name}}.types";
// export { canEdit } from "./{{kebabCase name}}.domain";
// export {
//   {{upperSnakeCase (singular name)}}_QUERY_KEYS,
//   use{{pascalCase name}}ListQuery,
//   use{{pascalCase (singular name)}}DetailQuery,
// } from "./{{kebabCase name}}.queries";

export {};
